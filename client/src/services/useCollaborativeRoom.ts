import AgoraRTC,{type IAgoraRTCClient,type IAgoraRTCRemoteUser,type ILocalTrack,type ILocalAudioTrack,type ILocalVideoTrack} from "agora-rtc-sdk-ng";
import { Room,RoomEvent,Track,type LocalTrack,type RemoteTrack,type RemoteTrackPublication } from "livekit-client";
import { useCallback,useEffect,useRef,useState } from "react";
import { connectSocket } from "./socket";
import { displayConstraints,readAgoraStats } from "./webrtc";
import type { AgoraCredentials,CameraPreset,FrameRate,JoinAck,Quality,RoomAck,RoomState,ScreenProvider,StreamStats,TokenAck } from "../types";

type Camera={identity:string;track:MediaStreamTrack;local:boolean;muted:boolean};
type ScreenAck=RoomAck&{error?:string};
type LiveKitAck={ok:boolean;livekitUrl?:string;livekitToken?:string;error?:string};
const initial:RoomState={live:false,count:0,activeScreenSharerId:null,activeScreenUid:null,screenProvider:"agora",livekitActive:false};
const valid=(track?:MediaStreamTrack|null)=>!!track&&track.readyState==="live";
const emitAck=<T,>(event:string,payload:object)=>new Promise<T>((resolve,reject)=>connectSocket().timeout(12_000).emit(event,payload,(error:Error|null,ack:T)=>error?reject(error):resolve(ack)));

export function useCollaborativeRoom(owner:boolean,requestedRoomId?:string){
  const videoRef=useRef<HTMLVideoElement>(null),streamRef=useRef<MediaStream|null>(null),subscriberRef=useRef<IAgoraRTCClient|null>(null),screenClientRef=useRef<IAgoraRTCClient|null>(null),screenTracksRef=useRef<(ILocalVideoTrack|ILocalAudioTrack)[]>([]),livekitRef=useRef<Room|null>(null),livekitPromiseRef=useRef<Promise<Room>|null>(null),livekitAudioRef=useRef<Map<string,HTMLMediaElement>>(new Map()),screenLivekitTracksRef=useRef<MediaStreamTrack[]>([]),credentialsRef=useRef<AgoraCredentials|null>(null),screenCredentialsRef=useRef<AgoraCredentials|null>(null),roomIdRef=useRef(requestedRoomId||""),stateRef=useRef<RoomState>(initial),cameraRef=useRef<MediaStreamTrack|null>(null),micRef=useRef<MediaStreamTrack|null>(null),fallbackTimerRef=useRef<ReturnType<typeof setTimeout>|null>(null),publishingRef=useRef(false),stoppingRef=useRef(false),socketIdRef=useRef(""),screenConfigRef=useRef<{quality:Quality;fps:FrameRate}>({quality:"1080p",fps:30});
  const [roomId,setRoomId]=useState(requestedRoomId||""),[credentials,setCredentials]=useState<AgoraCredentials|null>(null),[roomState,setRoomState]=useState<RoomState>(initial),[ready,setReady]=useState(false),[status,setStatus]=useState("Conectando"),[error,setError]=useState(""),[stats,setStats]=useState<StreamStats|null>(null),[cameras,setCameras]=useState<Camera[]>([]),[cameraOn,setCameraOn]=useState(false),[micOn,setMicOn]=useState(false),[cameraPreset,setCameraPreset]=useState<CameraPreset>("720p40"),[switching,setSwitching]=useState(false),[muted,setMuted]=useState(false);
  const updateState=useCallback((next:Partial<RoomState>)=>{stateRef.current={...stateRef.current,...next};setRoomState(stateRef.current);},[]);
  const putCamera=useCallback((camera:Camera)=>setCameras(current=>[...current.filter(item=>item.identity!==camera.identity),camera]),[]);
  const removeCamera=useCallback((identity:string)=>setCameras(current=>current.filter(item=>item.identity!==identity)),[]);
  const clearVideo=useCallback(()=>{if(videoRef.current)videoRef.current.srcObject=null;setStats(null);},[]);
  const showVideo=useCallback((track:MediaStreamTrack)=>{if(!videoRef.current)return;videoRef.current.srcObject=new MediaStream([track]);void videoRef.current.play().catch(()=>undefined);},[]);
  const renew=useCallback(async(client:IAgoraRTCClient,screen=false)=>{const ack=await emitAck<TokenAck>("renew-agora-token",{roomId:roomIdRef.current,screen});if(!ack.ok||!ack.agoraToken)throw new Error(ack.error||"Token Agora inválido.");await client.renewToken(ack.agoraToken);},[]);
  const ensureLivekit=useCallback(async(forceNew=false):Promise<Room>=>{if(!forceNew&&livekitRef.current&&livekitRef.current.state==="connected")return livekitRef.current;if(!forceNew&&livekitPromiseRef.current)return livekitPromiseRef.current;const pending=(async()=>{if(forceNew&&livekitRef.current){await livekitRef.current.disconnect(false);livekitRef.current=null;}const ack=await emitAck<LiveKitAck>("get-livekit-token",{roomId:roomIdRef.current});if(!ack.ok||!ack.livekitUrl||!ack.livekitToken)throw new Error(ack.error||"LiveKit indisponível.");const room=new Room({adaptiveStream:true,dynacast:true});const onSubscribed=(track:RemoteTrack,publication:RemoteTrackPublication,participant:{identity:string})=>{if(publication.source===Track.Source.Camera){putCamera({identity:participant.identity,track:track.mediaStreamTrack,local:false,muted:false});}else if(publication.source===Track.Source.ScreenShare&&stateRef.current.screenProvider==="livekit")showVideo(track.mediaStreamTrack);else if(track.kind===Track.Kind.Audio){const element=track.attach();element.autoplay=true;element.style.display="none";document.body.append(element);livekitAudioRef.current.set(publication.trackSid,element);}};const onUnsubscribed=(track:RemoteTrack,publication:RemoteTrackPublication,participant:{identity:string})=>{track.detach().forEach(element=>element.remove());if(publication.source===Track.Source.Camera)removeCamera(participant.identity);if(publication.source===Track.Source.ScreenShare&&stateRef.current.screenProvider==="livekit")clearVideo();const element=livekitAudioRef.current.get(publication.trackSid);element?.remove();livekitAudioRef.current.delete(publication.trackSid);};room.on(RoomEvent.TrackSubscribed,onSubscribed);room.on(RoomEvent.TrackUnsubscribed,onUnsubscribed);room.on(RoomEvent.ParticipantDisconned,participant=>removeCamera(participant.identity));await room.connect(ack.livekitUrl,ack.livekitToken);livekitRef.current=room;if(forceNew){if(valid(cameraRef.current))await room.localParticipant.publishTrack(cameraRef.current!,{source:Track.Source.Camera});if(valid(micRef.current))await room.localParticipant.publishTrack(micRef.current!,{source:Track.Source.Microphone});}return room;})();livekitPromiseRef.current=pending;try{return await pending;}finally{livekitPromiseRef.current=null;}},[clearVideo,putCamera,removeCamera,showVideo]);
  const fallback=useCallback(async():Promise<boolean>=>{const stream=streamRef.current;if(!stream||!valid(stream.getVideoTracks()[0])||stateRef.current.screenProvider==="livekit")return false;setSwitching(true);try{const ack=await emitAck<{ok:boolean;error?:string}>("screen-provider-changed",{roomId:roomIdRef.current,provider:"livekit"});if(!ack.ok)throw new Error(ack.error||"Fallback recusado.");updateState({screenProvider:"livekit",livekitActive:true});const agora=screenClientRef.current;if(agora){try{await agora.unpublish();await agora.leave();}catch(cause){console.error("Agora fallback cleanup error",cause);}screenClientRef.current=null;}const room=await ensureLivekit(true),video=stream.getVideoTracks().find(valid),audio=stream.getAudioTracks().find(valid);if(!video)throw new Error("Tela encerrada.");await room.localParticipant.publishTrack(video,{source:Track.Source.ScreenShare});screenLivekitTracksRef.current=[video];if(audio){await room.localParticipant.publishTrack(audio,{source:Track.Source.ScreenShareAudio});screenLivekitTracksRef.current.push(audio);}connectSocket().emit("broadcast-started",{roomId:roomIdRef.current});return true;}catch(cause){console.error("LiveKit screen fallback error",cause);setError("Não foi possível trocar o servidor de transmissão.");return false;}finally{setSwitching(false);}},[ensureLivekit,updateState]);
  const stopScreen=useCallback(async()=>{if(stoppingRef.current)return;stoppingRef.current=true;const stream=streamRef.current;streamRef.current=null;if(fallbackTimerRef.current)clearTimeout(fallbackTimerRef.current);fallbackTimerRef.current=null;const client=screenClientRef.current,tracks=screenTracksRef.current;screenClientRef.current=null;screenTracksRef.current=[];screenCredentialsRef.current=null;try{if(client){try{await client.unpublish();}finally{await client.leave();}}const room=livekitRef.current;if(room)for(const track of screenLivekitTracksRef.current)await room.localParticipant.unpublishTrack(track,false);screenLivekitTracksRef.current=[];}catch(cause){console.error("Screen cleanup error",cause);}finally{tracks.forEach(track=>track.close());stream?.getTracks().forEach(track=>track.stop());clearVideo();setMuted(false);connectSocket().emit("release-screen-share",{roomId:roomIdRef.current},()=>undefined);updateState({live:false,activeScreenSharerId:null,activeScreenUid:null,screenProvider:"agora"});stoppingRef.current=false;}},[clearVideo,updateState]);
  const publishAgora=useCallback(async(auth:AgoraCredentials,stream:MediaStream)=>{const client=AgoraRTC.createClient({mode:"live",codec:"vp8"});screenClientRef.current=client;client.on("connection-state-change",state=>{if(state==="CONNECTED"){if(fallbackTimerRef.current){clearTimeout(fallbackTimerRef.current);fallbackTimerRef.current=null;}if(streamRef.current===stream&&screenTracksRef.current.length&&!client.localTracks.length&&!publishingRef.current){publishingRef.current=true;void client.publish(screenTracksRef.current).catch(cause=>console.error("Agora screen republish error",cause)).finally(()=>publishingRef.current=false);}}else if(state==="DISCONNECTED"&&streamRef.current===stream&&stateRef.current.screenProvider==="agora"&&!fallbackTimerRef.current)fallbackTimerRef.current=setTimeout(()=>{if(client.connectionState!=="CONNECTED")void fallback();},9_000);});client.on("token-privilege-will-expire",()=>void renew(client,true).catch(cause=>console.error("Agora screen token renewal error",cause)));client.on("token-privilege-did-expire",()=>void renew(client,true).catch(cause=>console.error("Agora screen token expiry error",cause)));await client.setClientRole("host");await client.join(auth.agoraAppId,auth.agoraChannel,auth.agoraToken,auth.agoraUid);const video=stream.getVideoTracks().find(valid),audio=stream.getAudioTracks().find(valid);if(!video)throw new Error("A captura não forneceu vídeo ativo.");const settings=video.getSettings(),{quality,fps}=screenConfigRef.current,bitrateMax=quality==="1080p"?(fps===60?5000:3000):quality==="720p"?(fps===60?3000:2000):(fps===60?3500:2500),tracks:(ILocalVideoTrack|ILocalAudioTrack)[]=[AgoraRTC.createCustomVideoTrack({mediaStreamTrack:video,width:settings.width,height:settings.height,frameRate:fps,bitrateMax,optimizationMode:"motion"})];if(audio)tracks.push(AgoraRTC.createCustomAudioTrack({mediaStreamTrack:audio,encoderConfig:"music_standard"}));console.info("LumaCast capture",{video:settings,requestedFps:fps,bitrateMax,hasAudio:!!audio});screenTracksRef.current=tracks;await client.publish(tracks);},[fallback,renew]);
  const startScreen=useCallback(async(quality:Quality,fps:FrameRate)=>{setError("");screenConfigRef.current={quality,fps};if(stateRef.current.activeScreenSharerId&&stateRef.current.activeScreenSharerId!==socketIdRef.current){setError("Outra pessoa já está compartilhando a tela.");return;}const lock:ScreenAck=await emitAck<ScreenAck>("request-screen-share",{roomId:roomIdRef.current}).catch(cause=>({ok:false,error:String(cause)}));if(!lock.ok||!lock.agoraAppId||!lock.agoraChannel||lock.agoraUid===undefined||!lock.agoraToken){setError(lock.error||"Não foi possível iniciar a transmissão.");return;}const auth:AgoraCredentials={agoraAppId:lock.agoraAppId,agoraChannel:lock.agoraChannel,agoraUid:lock.agoraUid,agoraToken:lock.agoraToken};screenCredentialsRef.current=auth;updateState({activeScreenSharerId:socketIdRef.current,activeScreenUid:auth.agoraUid,screenProvider:"agora"});let stream:MediaStream;try{stream=await navigator.mediaDevices.getDisplayMedia(displayConstraints(quality,fps));}catch(cause){console.error("Screen capture error",cause);setError((cause as DOMException).name==="NotAllowedError"?"O compartilhamento foi cancelado.":"Não foi possível capturar a tela.");connectSocket().emit("release-screen-share",{roomId:roomIdRef.current},()=>undefined);return;}if(!valid(stream.getVideoTracks()[0])){stream.getTracks().forEach(track=>track.stop());connectSocket().emit("release-screen-share",{roomId:roomIdRef.current},()=>undefined);setError("Não foi possível capturar a tela.");return;}streamRef.current=stream;showVideo(stream.getVideoTracks()[0]);stream.getVideoTracks()[0].addEventListener("ended",()=>void stopScreen(),{once:true});try{await publishAgora(auth,stream);}catch(cause){console.error("Agora screen publish error",cause);if(!await fallback()){await stopScreen();return;}}updateState({live:true});connectSocket().emit("broadcast-started",{roomId:roomIdRef.current});},[fallback,publishAgora,showVideo,stopScreen,updateState]);
  const toggleCamera=useCallback(async()=>{try{const room=await ensureLivekit();if(cameraOn){await room.localParticipant.setCameraEnabled(false);cameraRef.current?.stop();cameraRef.current=null;removeCamera(socketIdRef.current);setCameraOn(false);}else{const config=cameraPreset==="480p60"?{width:854,height:480,frameRate:60}:{width:1280,height:720,frameRate:40},publication=await room.localParticipant.setCameraEnabled(true,{resolution:config,frameRate:config.frameRate});const track=publication?.track?.mediaStreamTrack;if(track){cameraRef.current=track;putCamera({identity:socketIdRef.current,track,local:true,muted:!micOn});setCameraOn(true);}}connectSocket().emit("livekit-media-active",{roomId:roomIdRef.current,active:!cameraOn||micOn||!!streamRef.current&&stateRef.current.screenProvider==="livekit"});}catch(cause){console.error("LiveKit camera error",cause);setError("Não foi possível ativar a câmera.");}},[cameraOn,cameraPreset,ensureLivekit,micOn,putCamera,removeCamera]);
  const toggleMic=useCallback(async()=>{try{const room=await ensureLivekit();if(micOn){await room.localParticipant.setMicrophoneEnabled(false);micRef.current?.stop();micRef.current=null;setMicOn(false);}else{const publication=await room.localParticipant.setMicrophoneEnabled(true);micRef.current=publication?.track?.mediaStreamTrack||null;setMicOn(true);}setCameras(current=>current.map(item=>item.local?{...item,muted:micOn}:item));connectSocket().emit("livekit-media-active",{roomId:roomIdRef.current,active:!micOn||cameraOn||!!streamRef.current&&stateRef.current.screenProvider==="livekit"});}catch(cause){console.error("LiveKit microphone error",cause);setError("Não foi possível ativar o microfone.");}},[cameraOn,ensureLivekit,micOn]);
  const toggleScreenAudio=useCallback(()=>{const nextMuted=!muted,sourceTracks=streamRef.current?.getAudioTracks().filter(valid)||[],agoraAudio=screenTracksRef.current.filter((track):track is ILocalAudioTrack=>track.trackMediaType==="audio");if(!sourceTracks.length){setError("Esta captura não possui áudio para silenciar.");return;}sourceTracks.forEach(track=>{track.enabled=!nextMuted;});agoraAudio.forEach(track=>track.setVolume(nextMuted?0:100));setMuted(nextMuted);},[muted]);
  useEffect(()=>{const socket=connectSocket();const apply=(ack:RoomAck|JoinAck)=>{if(!ack.ok||!ack.agoraAppId||!ack.agoraChannel||ack.agoraUid===undefined||!ack.agoraToken){setError(ack.error||"Sala não encontrada.");setStatus("missing");return;}const next:AgoraCredentials={agoraAppId:ack.agoraAppId,agoraChannel:ack.agoraChannel,agoraUid:ack.agoraUid,agoraToken:ack.agoraToken};credentialsRef.current=next;setCredentials(next);updateState({live:!!ack.live,count:ack.count||0,activeScreenSharerId:ack.activeScreenSharerId||null,activeScreenUid:ack.activeScreenUid??null,screenProvider:ack.screenProvider||"agora",livekitActive:!!ack.livekitActive});if("roomId" in ack&&ack.roomId){roomIdRef.current=ack.roomId;setRoomId(ack.roomId);}if("ownerToken" in ack&&ack.ownerToken)sessionStorage.setItem("lumacast-broadcaster",JSON.stringify({roomId:roomIdRef.current,token:ack.ownerToken}));if("participantToken" in ack&&ack.participantToken)sessionStorage.setItem(`lumacast-participant-${roomIdRef.current}`,ack.participantToken);if(screenCredentialsRef.current&&streamRef.current&&socket.id===stateRef.current.activeScreenSharerId&&screenClientRef.current?.connectionState==="DISCONNECTED")void publishAgora(screenCredentialsRef.current,streamRef.current).catch(()=>void fallback());};const connect=()=>{socketIdRef.current=socket.id||"";setStatus("Conectado");if(owner){const saved=sessionStorage.getItem("lumacast-broadcaster");if(saved){try{const parsed=JSON.parse(saved) as {roomId:string;token:string};socket.emit("reclaim-room",parsed,(ack:RoomAck)=>ack.ok?apply(ack):socket.emit("create-room",(fresh:RoomAck)=>apply(fresh)));}catch{socket.emit("create-room",(ack:RoomAck)=>apply(ack));}}else socket.emit("create-room",(ack:RoomAck)=>apply(ack));}else{socket.emit("join-room",{roomId:requestedRoomId,participantToken:sessionStorage.getItem(`lumacast-participant-${requestedRoomId}`)},(ack:JoinAck)=>apply(ack));}};const onState=(next:RoomState)=>{const previous=stateRef.current;updateState(next);if(!next.live&&previous.live&&socket.id!==previous.activeScreenSharerId)clearVideo();if(next.screenProvider!==previous.screenProvider){subscriberRef.current?.remoteUsers.forEach(user=>user.audioTrack?.stop());if(socket.id===next.activeScreenSharerId&&valid(streamRef.current?.getVideoTracks()[0]))showVideo(streamRef.current!.getVideoTracks()[0]);else clearVideo();setSwitching(next.screenProvider==="livekit");if(next.screenProvider==="livekit")void ensureLivekit().finally(()=>setSwitching(false));}};const onDisconnect=()=>setStatus("Reconectando");socket.on("connect",connect);socket.on("disconnect",onDisconnect);socket.on("room-state",onState);socket.on("room-expired",()=>setStatus("missing"));if(socket.connected)connect();return()=>{socket.off("connect",connect);socket.off("disconnect",onDisconnect);socket.off("room-state",onState);socket.off("room-expired");};},[clearVideo,ensureLivekit,fallback,owner,publishAgora,requestedRoomId,showVideo,updateState]);
  useEffect(()=>{if(!credentials)return;let active=true;const client=AgoraRTC.createClient({mode:"live",codec:"vp8"});subscriberRef.current=client;const subscribe=async(user:IAgoraRTCRemoteUser,mediaType:"video"|"audio")=>{if(stateRef.current.screenProvider!=="agora"||user.uid!==stateRef.current.activeScreenUid)return;try{await client.subscribe(user,mediaType);if(!active)return;if(mediaType==="video"&&user.videoTrack&&socketIdRef.current!==stateRef.current.activeScreenSharerId)showVideo(user.videoTrack.getMediaStreamTrack());if(mediaType==="audio"&&user.audioTrack&&socketIdRef.current!==stateRef.current.activeScreenSharerId)user.audioTrack.play();}catch(cause){console.error("Agora screen subscribe error",cause);setError("Não foi possível receber a tela.");}};client.on("user-published",subscribe);client.on("user-unpublished",(user,type)=>{if(user.uid===stateRef.current.activeScreenUid&&type==="video"&&socketIdRef.current!==stateRef.current.activeScreenSharerId)clearVideo();});client.on("token-privilege-will-expire",()=>void renew(client).catch(cause=>console.error("Agora token renewal error",cause)));client.on("token-privilege-did-expire",()=>void renew(client).catch(cause=>console.error("Agora token expiry error",cause)));void (async()=>{try{await client.setClientRole("audience");await client.join(credentials.agoraAppId,credentials.agoraChannel,credentials.agoraToken,credentials.agoraUid,{autoSubscribe:false});if(active)setReady(true);}catch(cause){console.error("Agora viewer join error",cause);if(active){setReady(false);setError("Não foi possível conectar ao servidor principal. A tela poderá usar o servidor alternativo.");}}})();return()=>{active=false;setReady(false);if(subscriberRef.current===client)subscriberRef.current=null;void client.leave().catch(()=>undefined);};},[credentials?.agoraAppId,credentials?.agoraChannel,credentials?.agoraUid,clearVideo,renew,showVideo]);
  useEffect(()=>{if(roomState.livekitActive){void ensureLivekit().catch(cause=>{console.error("LiveKit join error",cause);setError("Não foi possível conectar ao áudio e vídeo da sala.");});return;}const timer=setTimeout(()=>{if(!stateRef.current.livekitActive&&!valid(cameraRef.current||undefined)&&!valid(micRef.current||undefined)&&!(streamRef.current&&stateRef.current.screenProvider==="livekit")){const room=livekitRef.current;livekitRef.current=null;if(room)void room.disconnect();setCameras([]);livekitAudioRef.current.forEach(element=>element.remove());livekitAudioRef.current.clear();}},3_000);return()=>clearTimeout(timer);},[roomState.livekitActive,ensureLivekit]);
  useEffect(()=>{if(!roomState.live)return;const timer=setInterval(()=>{const client=socketIdRef.current===roomState.activeScreenSharerId?screenClientRef.current:subscriberRef.current;if(client&&roomState.screenProvider==="agora")void readAgoraStats(client,streamRef.current,socketIdRef.current===roomState.activeScreenSharerId?null:roomState.activeScreenUid).then(setStats);},1000);return()=>clearInterval(timer);},[roomState.live,roomState.activeScreenSharerId,roomState.activeScreenUid,roomState.screenProvider]);
  useEffect(()=>()=>{const room=livekitRef.current;if(room)void room.disconnect();livekitAudioRef.current.forEach(element=>element.remove());cameraRef.current?.stop();micRef.current?.stop();if(streamRef.current)void stopScreen();},[stopScreen]);
  return{videoRef,roomId,roomState,ready,status,error,setError,stats,cameras,cameraOn,micOn,cameraPreset,setCameraPreset,switching,muted,isScreenSharer:!!roomState.activeScreenSharerId&&roomState.activeScreenSharerId===socketIdRef.current,startScreen,stopScreen,toggleCamera,toggleMic,toggleScreenAudio};
}
