import { BarChart3,Check,ChevronDown,Clipboard,Eye,MonitorUp,Radio,Square,Users,Video,VideoOff,Volume2,VolumeX,WifiOff } from "lucide-react";
import { useState } from "react";
import { BrowserCompatibilityNotice } from "../components/BrowserCompatibilityNotice";
import { Logo } from "../components/Logo";
import { OptimizedVideo } from "../components/OptimizedVideo";
import { ParticipantsSidebar } from "../components/ParticipantsSidebar";
import { RoomStage } from "../components/RoomStage";
import { StatsPanel } from "../components/StatsPanel";
import { StatusPill } from "../components/StatusPill";
import { useAdaptiveScreenQuality } from "../hooks/useAdaptiveScreenQuality";
import { useMediaPreferences } from "../hooks/useMediaPreferences";
import { copyText } from "../services/browser";
import { useCollaborativeRoom } from "../services/useCollaborativeRoom";
import type { CameraPreset,Quality } from "../types";

export function BroadcasterPage(){
  const room=useCollaborativeRoom(true),{quality,setQuality,fps,setFps}=useMediaPreferences(),[copied,setCopied]=useState(false),[showStats,setShowStats]=useState(false);
  useAdaptiveScreenQuality({enabled:quality==="auto"&&room.isScreenSharer&&room.roomState.screenProvider==="agora",stats:room.stats,preferredFps:fps,updateQuality:room.updateScreenQuality,updateFrameRate:room.updateScreenFrameRate});
  const roomUrl=room.roomId?`${location.origin}/?room=${encodeURIComponent(room.roomId)}`:"",screenStarting=!!room.roomState.activeScreenSharerId&&!room.roomState.live,busy=!!room.roomState.activeScreenSharerId&&!room.ownsScreenLock,canShare=typeof navigator.mediaDevices?.getDisplayMedia==="function",stageTitle=screenStarting?(room.ownsScreenLock?"Preparando sua transmissão…":(room.roomState.activeScreenSharerName||"Alguém")+" está iniciando uma transmissão…"):"Nenhuma tela sendo compartilhada",stageText=screenStarting?(room.ownsScreenLock?"Escolha a tela ou janela que deseja compartilhar.":"Aguarde enquanto a tela é preparada."):"Qualquer participante pode começar a transmitir.";
  const copyInvite=async()=>{try{await copyText(roomUrl);setCopied(true);setTimeout(()=>setCopied(false),1800);}catch{room.setError("Não foi possível copiar o convite.");}};
  return <main className="studio-shell"><header className="studio-nav"><Logo/><div className="room-head"><span>SALA</span><b>{room.roomId||"CRIANDO…"}</b></div><div className="nav-status"><StatusPill label={room.roomState.live?"AO VIVO":"PRONTO"} tone={room.roomState.live?"live":"neutral"}/><span className={`nav-connection ${room.status==="Conectado"?"online":""}`} title={room.status==="Conectado"?"Servidor de sinalização online":"Tentando restabelecer conexão"}><i/>{room.status}</span><span className="viewer-count"><Users/>{room.roomState.count}</span></div></header><BrowserCompatibilityNotice/>
    <section className="studio-main collaborative-layout"><div className="studio-stage"><RoomStage cameras={room.cameras} participants={room.roomState.participants}><div className={`broadcast-screen ${room.roomState.live?"has-video":""}`}><OptimizedVideo videoRef={room.videoRef} muted={room.isScreenSharer}/><div className="stage-placeholder"><span><MonitorUp/></span><h2>{stageTitle}</h2><p>{stageText}</p></div>{room.roomState.live&&<div className="live-corner"><i/>AO VIVO</div>}{room.switching&&<div className="provider-switch">Trocando servidor de transmissão…</div>}</div></RoomStage>
    </div><aside className="studio-side"><div className="side-card invite-card"><div className="card-title"><span><Eye/></span><div><b>Convide espectadores</b><small>Compartilhe este link</small></div></div><div className="share-code"><span>{room.roomId||"—"}</span><button type="button" onClick={()=>void copyInvite()} disabled={!roomUrl} aria-label={copied?"Convite copiado":"Copiar link de convite"} title={copied?"Convite copiado":"Copiar link de convite"}>{copied?<Check/>:<Clipboard/>}</button></div><p aria-live="polite">{copied?"Link copiado para a área de transferência.":"O código também pode ser digitado na página inicial."}</p></div>
      <div className="side-card settings-card"><div className="card-title"><span><Radio/></span><div><b>Qualidade</b><small>{room.isScreenSharer?"Ajuste ao vivo":"Defina antes de iniciar"}</small></div></div><label>RESOLUÇÃO<div className="select-wrap"><select aria-label="Resolução da tela" value={quality} onChange={e=>{const next=e.target.value as Quality;setQuality(next);if(room.isScreenSharer){void room.updateScreenQuality(next);if(next!=="auto")void room.updateScreenFrameRate(fps);}}}><option value="auto">Automática</option><option value="720p">720p HD</option><option value="1080p">1080p Full HD</option></select><ChevronDown/></div></label><label>QUADROS POR SEGUNDO<div className="segmented"><button type="button" aria-pressed={fps===30} className={fps===30?"active":""} onClick={()=>{setFps(30);if(room.isScreenSharer)void room.updateScreenFrameRate(30);}}>30 FPS</button><button type="button" aria-pressed={fps===60} className={fps===60?"active":""} onClick={()=>{setFps(60);if(room.isScreenSharer)void room.updateScreenFrameRate(60);}}>60 FPS</button></div></label><label>CÂMERA<div className="select-wrap"><select aria-label="Qualidade da câmera" value={room.cameraPreset} onChange={e=>room.setCameraPreset(e.target.value as CameraPreset)} disabled={room.cameraOn}><option value="720p40">720p · 40 FPS</option><option value="480p60">480p · 60 FPS</option></select><ChevronDown/></div></label></div>
      <div className="side-card studio-controls-card"><div className="card-title"><span><MonitorUp/></span><div><b>Controles</b><small>Tela, câmera e estatísticas</small></div></div><div className="control-dock side-controls"><button className="control" onClick={()=>void room.toggleCamera()}>{room.cameraOn?<Video/>:<VideoOff/>}<span>Câmera</span></button>{room.isScreenSharer?<button className="stop-live" onClick={()=>void room.stopScreen()}><Square/>Parar transmissão</button>:<button className="go-live" onClick={()=>void room.startScreen(quality,fps)} disabled={!room.roomId||busy||!canShare} title={!canShare?"Compartilhamento de tela não é suportado neste navegador.":undefined}><Radio/>{!canShare?"Tela indisponível":busy?"Tela ocupada":"Compartilhar tela"}</button>}{room.isScreenSharer&&<button className="control" onClick={()=>void room.toggleScreenAudio()}>{room.muted?<VolumeX/>:<Volume2/>}<span>{room.muted?"Ativar áudio":"Silenciar áudio"}</span></button>}<button className={`control ${showStats?"active":""}`} onClick={()=>setShowStats(!showStats)}><BarChart3/><span>Estatísticas</span></button></div></div>
      </aside><ParticipantsSidebar participants={room.roomState.participants}/></section>
    {showStats&&<section className="stats-drawer" aria-live="polite"><h3>Estatísticas da conexão</h3><StatsPanel stats={room.stats}/></section>}{room.error&&<div className="toast-error" role="alert"><WifiOff/>{room.error}<button type="button" aria-label="Fechar aviso" onClick={()=>room.setError("")}>×</button></div>}
  </main>;
}
